[package]
name = "durable-bindings"
version = "0.1.0"
edition = "2018"

[dependencies]
fp-bindgen-support = { version = "3.0.0", features = ["async", "guest"] }
once_cell = { version = "1" }
rmp-serde = { version = "1.0" }
serde = { version = "1.0", features = ["derive"] }
